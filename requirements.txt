# Core web stack
Flask==3.1.1
Flask-SQLAlchemy==3.1.1
Flask-Login==0.6.3
Flask-WTF==1.3.0
WTForms==3.2.2
# Required by WTForms' Email() validator - without it the employee form raises
email-validator>=2.2
SQLAlchemy==2.0.51

# MySQL driver ("cryptography" is needed for MySQL 8 caching_sha2_password)
PyMySQL==1.1.1
cryptography>=44.0

# Passwords
bcrypt>=4.2

# Face detection + recognition (OpenCV DNN only: no torch / onnxruntime needed)
opencv-python-headless>=4.10
numpy>=2.0

# The payroll master sheet is a real .xlsx workbook with live formulas
openpyxl>=3.1

# Config + production server
python-dotenv==1.1.1
waitress==3.0.2

# Windows has no system tz database, so zoneinfo needs this
tzdata>=2024.1

# Tests
pytest==8.4.2
